<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= yield(:title) %> | <%= t("app.title") %>
    </title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track": "reload" %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
